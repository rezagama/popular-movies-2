<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="title"
            type="String" />
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <View style="@style/GreyDivider" />
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/dimen_padding_small">
            <TextView
                android:text="@{title}"
                android:textSize="16sp"
                android:textColor="@color/textPrimary"
                android:drawablePadding="@dimen/dimen_padding_default"
                android:drawableLeft="@drawable/ic_stars_black_18dp"
                android:drawableStart="@drawable/ic_stars_black_18dp"
                android:padding="@dimen/dimen_padding_default"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                tools:text="Movie"/>
        </LinearLayout>
        <View style="@style/GreyDivider" />
    </LinearLayout>
</layout>